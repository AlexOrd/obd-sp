# This repo is public and its CI installs with no private-registry credentials.
# Pin the public registry so a contributor whose global npm points at a private
# proxy cannot bake proxy URLs into package-lock.json and break `npm ci` in CI.
registry=https://registry.npmjs.org/
